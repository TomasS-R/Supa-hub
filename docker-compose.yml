services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    networks:
      - dokploy-network
    restart: unless-stopped

networks:
  dokploy-network:
    external: true
